services:
  - type: web
    name: keio-inventory-demo
    runtime: python
    plan: free
    rootDir: .
    buildCommand: pip install -r prod_requirements.txt
    startCommand: bash start.sh
    autoDeploy: false
    healthCheckPath: /api/v1/health
